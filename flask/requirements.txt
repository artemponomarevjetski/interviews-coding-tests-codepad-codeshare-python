# --- Web server -------------------------------------------------
Flask==2.3.2           # pulls in Werkzeug, Jinja2, click, itsdangerous

# --- Imaging & OCR ---------------------------------------------
Pillow~=11.3           # provides PIL.Image, ImageFilter, ImageEnhance
pytesseract==0.3.13    # Python bindings; you still need the tesseract binary

# --- macOS screen‑capture helper --------------------------------
pyobjc-framework-Quartz ; platform_system == "Darwin"
